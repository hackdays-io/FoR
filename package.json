{
  "name": "for-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "FoR monorepo managed with pnpm",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "keywords": [
    "pnpm",
    "monorepo",
    "frontend",
    "contract"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.18.0"
  },
  "packageManager": "pnpm@9.12.0",
  "devDependencies": {
    "@biomejs/biome": "^2.3.7"
  }
}
