type TransferViaRouter @entity(immutable: true) {
  id: ID!
  sender: User!
  from: User!
  to: User!
  totalAmount: BigInt!
  fundAmount: BigInt!
  burnAmount: BigInt!
  recipientAmount: BigInt!
  message: String!
  blockNumber: BigInt!
  timestamp: BigInt!
  transactionHash: Bytes!
  logIndex: BigInt!
}

type Transfer @entity(immutable: true) {
  id: ID!
  from: User!
  to: User!
  value: BigInt!
  blockNumber: BigInt!
  timestamp: BigInt!
  transactionHash: Bytes!
  logIndex: BigInt!
}

type DistributionRatio @entity(immutable: true) {
  id: ID!
  changedBy: User!
  fundRatio: BigInt!
  burnRatio: BigInt!
  recipientRatio: BigInt!
  blockNumber: BigInt!
  timestamp: BigInt!
  transactionHash: Bytes!
  logIndex: BigInt!
}

type User @entity(immutable: false) {
  id: ID!
  totalTaxPaid: BigInt!
  totalFundTaxPaid: BigInt!
  totalBurnTaxPaid: BigInt!
  sentTransfersViaRouter: [TransferViaRouter!]! @derivedFrom(field: "from")
  receivedTransfersViaRouter: [TransferViaRouter!]! @derivedFrom(field: "to")
  executedTransfersViaRouter: [TransferViaRouter!]! @derivedFrom(field: "sender")
  sentTransfers: [Transfer!]! @derivedFrom(field: "from")
  receivedTransfers: [Transfer!]! @derivedFrom(field: "to")
  ratioChanges: [DistributionRatio!]! @derivedFrom(field: "changedBy")
}

type AllowedUser @entity(immutable: false) {
  id: ID!
  isAllowed: Boolean!
  addedAtBlock: BigInt!
  addedAtTimestamp: BigInt!
  updatedAtBlock: BigInt!
  updatedAtTimestamp: BigInt!
  updatedAtTransactionHash: Bytes!
}
